# Only install native binaries for this Mac (arm64) — avoids 30+ empty
# platform stub folders that make webpack crawl node_modules on slow drives.
supportedArchitectures.os=darwin
supportedArchitectures.cpu=arm64
